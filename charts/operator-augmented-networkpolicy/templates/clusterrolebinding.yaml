apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "operator-augmented-networkpolicy.fullname" . }}-manager-rolebinding
  labels:
    {{- include "operator-augmented-networkpolicy.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "operator-augmented-networkpolicy.fullname" . }}-manager-role
subjects:
  - kind: ServiceAccount
    name: {{ include "operator-augmented-networkpolicy.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
